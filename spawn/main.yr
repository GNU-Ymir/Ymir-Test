mod main;
import core.io;
import thread, core.string;

def test (a : int, b : char) {
    println (a, " ", b);
}

def main () {
    //let str = "salut".dup ();
    let a = spawn (100U, test, 1, 't');
    a.join ();
}
