import std::config::args;
import std::fs::file;
import std::io;
import std::conv;

struct
| mut f : f64
| mut z : [i64]
| mut u : [c32]
 -> X;


def main ()
{
    let cfg = parse ("./a.out -z 1 2 3 -f 89.9 -u test");
    println (cfg);
    let z = cfg.to!(X) ();
    println ("F -> ", z.f);
    println ("Z -> ", z.z);
    println ("U -> ", z.u);
} catch {
    x : _ => {
        println (x);
    }
}
