import std::io;

def ackermann (m : u64, n : u64) -> u64 {
    if m == 0_u64
        n + 1_u64
    else if n == 0_u64
        ackermann (m - 1_u64, 1_u64)
    else ackermann (m - 1_u64, ackermann (m, n - 1_u64))
}

def main () {
    println (ackermann (2_u64, 4_u64));
}

