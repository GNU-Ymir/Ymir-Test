import std::io;

def replicate (T of [U], U) (a : T, N : u64) -> mut [mut U] {
    let mut res : [mut U] = copy (a);
    for _ in 1u64 .. N {
        res = alias (res ~ a);
    }
    alias res
}

def main () {
    println ("ha".replicate (5u64));
}
