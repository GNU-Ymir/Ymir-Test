import std::io

def add_one (x : i32)-> i32 {
    if x == 12 {
        return x + 1;
    } else {
        return x + 2;
    }
}

def main () {
    let x = add_one (5);
    println ("The value of x : ", x);
}
