import core.io;
import std.conv;
import core.string;


def main () {
    let a = "salut".dup ();
    a [0] = 't';
    a [0 .. 2] = "ui";
    println (a, " ", a [0 .. 4]);


    if a [0 .. 2] > a [2 .. 5] {
        println ("ici");
    }

    if a [0 .. 3] == "uil"
        println ("la");
    
}
