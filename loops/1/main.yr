import std::io

def main () {
    let mut i = 0;
    let x = loop  {
        i += 1;	// the value of the while loop will be equal to 'i' at the last iteration	
        if (i >= 10) break {i}
    };
    
    println ("X is : ", x);
}
