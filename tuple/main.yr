import std::io;

def test (T...) () -> T {
    let mut a : (mut T) = T::init;
    for ref mut i in a {
        i = read!(typeof i);
        read!c8 (); // skip the return
    }
    a
}

def main () {
    let a = test!(i32, i32, c8);
    let (c, d, e) = a;
    println (c, ' ', d, ' ', e);
}
