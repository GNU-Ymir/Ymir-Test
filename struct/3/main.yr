import std::io;

class Z {
    pub let mut x = 1;
    pub self () {}

    impl Copiable;
}

struct
| dmut a : &Z
 -> X;
    
def foo (a : X)-> mut X {
    X (dcopy a.a)
}

def main ()
{
    let a = X (Z::new ());
    let mut b = foo (a);
    b.a.x = 98;
    println (a.a.x, " ", b.a.x);
} 
