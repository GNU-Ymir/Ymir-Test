
extern (C) printf (const c : p!char, ...) -> int;

def main (args : [[char]]) {
    printf ("Hello world\n".ptr);
    printf ("args.len = %d\n".ptr, args.len);
    for i in 0U .. args.len
        printf ("args [%d] = '%.*s'\n".ptr, i, args [i].len, args[i].ptr);
    
    return 0;
}
